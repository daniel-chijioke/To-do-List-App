# To-Do List Application 📝

A modern, responsive task management application that helps users organize their daily goals. This project focuses on CRUD operations and persistent data storage using the browser's LocalStorage.

## 🚀 Live Demo
[View Live Site](https://daniel-chijioke.github.io/To-do-list-App/)

## ✨ Key Features
* **Task Persistence:** Uses `localStorage` to ensure your tasks remain saved even after closing the browser or refreshing the page.
* **Interactive UI:** Users can add tasks by clicking the "Add" button or pressing the "Enter" key.
* **Task Management:** Features a toggle system to mark tasks as completed and a dedicated delete function to remove items from the list.
* **Custom Assets:** Includes a custom-designed logo, menu icon, and favicon for a branded user experience.
* **Responsive Design:** Styled with CSS Flexbox to ensure the layout remains clean on mobile devices and desktops.

## 🛠️ Technologies Used
* **HTML5:** Structured with a dedicated container for input fields and task lists.
* **CSS3:** Custom styling featuring the Poppins font family and polished UI components.
* **JavaScript (ES6):** Implements event listeners, DOM manipulation, and JSON-based data storage.

## 📂 Project Structure
* `/scripts/script.js` - Logic for managing tasks and browser storage.
* `/styles/style.css` - Custom styles and responsive layout rules.
* `index.html` - The core structure of the application.
* `logo.png` & `favicon.jpg` - Visual branding assets.

## 🧠 What I Learned
During the development of this app, I mastered how to use `JSON.stringify()` and `JSON.parse()` to store complex data structures in a user's browser, allowing for a seamless experience without a backend database.
